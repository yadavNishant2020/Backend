<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>EJS Tags</title>
</head>

<body>
  <h1>
    <!-- Title goes here -->
    EJS Tags
  </h1>
  <p>Current second:
    <!-- Current second goes here -->
    <%
    const nDate = new Date();
    const seconds = nDate.getSeconds();
    %>
    <%= seconds %>
  </p>

  <% if (seconds % 2 === 0) { %>
    <ul>
      <!-- List goes here if the current second is even. -->
      <% for (let i = 0; i < items.length; i++) { %>
        <li><%= items[i] %></li>
      <% } %>
    </ul>
  <% } else { %>
    <p>No items to display</p>
  <% } %>


  <p>
    <!-- HTML content goes here -->
    <%- htmlContent %>
  </p>

  <!-- Footer goes here -->
  <%- include("footer.ejs") %>
</body>

</html>
